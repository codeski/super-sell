<h1>Product Show</h1>

<h3><%= @product.name %>, Price: $<%= @product.price.to_s %>, Brand: <%= @product.brand.name %>, Department: <%= @product.department.name %></h3>
    <%= link_to 'All Products', products_path %>, <%= link_to 'Edit', edit_product_path(@product) %>, <%= link_to 'Delete', @product, :confirm => 'Are you sure?', :method => :delete %>